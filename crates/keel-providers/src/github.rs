//! GitHub: repository discovery, cloning, and pull requests.

/// Placeholder for the authenticated GitHub client.
///
/// Deliberately empty at Milestone 0 — the scanner ships before any network access exists, so this
/// stays a type and a doc comment until Milestone 2.
#[derive(Debug, Default)]
pub struct GitHub;
