/target
node_modules/
dist/
.DS_Store

# Quarantined repository-supplied agent config. Never committed: it is untrusted by definition.
.keel/quarantine/

# What someone dropped or pasted into the composer. Belongs to the machine, not the repository.
.keel/attachments/

.env
.env.*
!.env.example
ui/node_modules/

# SwiftPM's build directory: four thousand files, and the Changes panel counts every one of them.
app/.build/

# Sparkle command-line tools, fetched by make sparkle-tools
packaging/sparkle/
.env
