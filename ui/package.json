{
  "name": "keel-ui",
  "private": true,
  "type": "module",
  "description": "The UI is one HTML file with no build step. This exists for check.mjs and for the record below.",

  "//vendored": "What ui/vendor was built from. These are not installed — the files are committed, because the binary embeds them with rust-embed and a build that downloads 14MB of editor is not a build worth having. Re-vendor from these versions.",
  "vendored": {
    "monaco-editor": "0.54.0",
    "xterm": "5.3.0",
    "xterm-addon-fit": "0.8.0"
  },

  "devDependencies": {
    "happy-dom": "^20"
  }
}
